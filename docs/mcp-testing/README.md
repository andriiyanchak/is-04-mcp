# MCP A/B Tests

One file per MCP. Use the template below.

```markdown
## MCP Test: <mcp-name>

### Prompt
"<the same prompt for both runs>"

### Result WITH MCP (enabled)
- Tools called:
- Output summary:

### Result WITHOUT MCP (disabled: true, or removed from .cursor/mcp.json)
- Tools available:
- Output summary:

### Conclusion
What concrete difference did the MCP make? Be specific — "faster" / "more accurate" alone is not enough.
```

Save each test as `<mcp-name>.md` in this folder.
